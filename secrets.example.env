# Local configuration
#
# Copy this file to .env and customize:
#   cp secrets.example.env .env
#
# This project runs 100% locally by default.
# No API keys required for basic usage.

# =============================================================================
# REQUIRED: Directory configuration
# =============================================================================

# Your home directory (absolute path - keep this secret!)
HOME_DIR=/Users/yourname

# Project directory (relative to HOME_DIR)
PROJECT_DIR=dev/projects/screenshot-analyzer

# Directories to scan for screenshots (relative to HOME_DIR, colon-separated)
# Only these exact directories are scanned - no recursion into subdirectories
SOURCE_DIRS=screenshots

# =============================================================================
# OPTIONAL: API keys for cloud backends (not needed for local usage)
# =============================================================================

# ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=sk-...
